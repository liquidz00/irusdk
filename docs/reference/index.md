# API reference

```{toctree}
:hidden:

clients
services
models
exceptions
```

| Page | Contents |
| --- | --- |
| {doc}`clients` | `IruClient`, `AsyncIruClient`, `IruConfig`, `Region`, `Page` |
| {doc}`services` | The device, blueprint, user, tag, and library services |
| {doc}`models` | Pydantic models for every response shape |
| {doc}`exceptions` | The `IruError` hierarchy |
